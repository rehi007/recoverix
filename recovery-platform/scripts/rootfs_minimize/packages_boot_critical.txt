# Boot / runtime critical package name patterns (one per line).
# If a purge candidate matches any pattern → immediate ERROR.
# If apt -s purge simulation REMV's an installed package matching → ERROR.
#
# Kernel packages: NOT listed here — protected dynamically from config.env
# KEEP_KERNEL_FLAVOR via lib/boot_critical.sh (only that flavor; old kernels purge OK).

grub
grub-*
shim*
initramfs-tools*
systemd
bash
coreutils
util-linux
mount
libc6
python3
python3-minimal
python3-gi
python3-gi-cairo
gir1.2-gtk-3.0
gir1.2-glib-2.0
libgtk-3-0
libgtk-3-common
libglib2.0-0
partclone
ntfs-3g
ntfsprogs
e2fsprogs
efibootmgr
shim-signed
grub-efi*
grub-pc*
grub2-common
busybox-initramfs
dpkg
apt
tar
gzip
sed
grep
