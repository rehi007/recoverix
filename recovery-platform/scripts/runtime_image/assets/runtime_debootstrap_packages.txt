# Minimal Recoverix Runtime seed packages (Ubuntu 22.04 jammy).
# One package per line; # comments ignored.
# Installed one-by-one via recoverix_runtime_apt_install (strict minimal policy).
# Target rootfs size: 1–3 GiB after seed install (before recoverix python copy).

# Base / init
systemd
systemd-sysv
udev
dbus
libc6
bash
coreutils
grep
sed
gawk
findutils
tar
gzip
openssl
ca-certificates
locales
sudo

# Python + GTK Recovery UI (minimal; deps pulled without recommends)
python3
python3-gi
python3-gi-cairo
gir1.2-gtk-3.0
libgtk-3-0
fonts-noto-cjk

# Backup / filesystem tools
# ntfsprogs obsolete on jammy — see runtime_package_release_map.txt
# partclone in universe — see runtime_apt_repositories.sh
partclone
ntfs-3g
e2fsprogs
dosfstools
gdisk
parted
fdisk
util-linux
mount

# Boot / initramfs (kernel flavor installed separately)
grub-common
grub-efi-amd64-bin
grub-efi-amd64-signed
grub2-common
efibootmgr
shim-signed
initramfs-tools
initramfs-tools-core
initramfs-tools-bin
busybox-initramfs

# Minimal networking
iproute2
iputils-ping

# X11 / display (full GUI stack installed via runtime_gui_stack_install.sh)
xserver-xorg-core
xserver-xorg-video-dummy
libx11-6
dbus-x11

# GUI session stack — see assets/runtime_gui_packages.txt (gdm3, openbox, xorg, …)
